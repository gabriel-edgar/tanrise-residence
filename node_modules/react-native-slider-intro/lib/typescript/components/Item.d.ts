/// <reference types="react" />
import { IItem } from '../interfaces/IItem.interface';
declare const Item: ({ index, backgroundColor, title, text, link, activeLanguage, image, slideMaxHeightPercent, }: IItem) => JSX.Element;
export default Item;
