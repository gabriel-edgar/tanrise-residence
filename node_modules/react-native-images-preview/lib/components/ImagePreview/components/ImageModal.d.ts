import React from 'react';
import type { ImageModalProps } from '../Types';
declare const ImageModal: ({ setModalConfig, modalConfig, renderHeader, imageSource, doubleTapZoomEnabled, pinchZoomEnabled, swipeDownCloseEnabled, }: ImageModalProps) => React.JSX.Element;
export default ImageModal;
