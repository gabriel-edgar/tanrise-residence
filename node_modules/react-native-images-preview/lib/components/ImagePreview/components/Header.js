import React from 'react';
import { Text, TouchableOpacity } from 'react-native';
import Animated from 'react-native-reanimated';
import { Strings } from '../../../constants';
import { HeaderStyle as styles } from './styles';
const Header = ({ renderHeader, onPressClose, headerOpacityAnimation, }) => {
    return (React.createElement(Animated.View, { style: [styles.closeButtonParent, headerOpacityAnimation] }, renderHeader ? (renderHeader(onPressClose)) : (React.createElement(TouchableOpacity, { style: styles.closeButton, onPress: () => {
            onPressClose();
        } },
        React.createElement(Text, { style: styles.closeText }, Strings.close)))));
};
export default Header;
//# sourceMappingURL=Header.js.map