import React from 'react';
import { ActivityIndicator } from 'react-native';
import { ImageLoaderStyle as styles } from './styles';
const ImageLoader = ({ renderImageLoader, ...rest }) => {
    return renderImageLoader ? (renderImageLoader()) : (React.createElement(ActivityIndicator, { style: styles.activityIndicatorStyle, ...rest }));
};
export default ImageLoader;
//# sourceMappingURL=ImageLoader.js.map